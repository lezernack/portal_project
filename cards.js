let cardsList = document.querySelector(".cards");
let cards = [];

const project = {
  url: "https://lezernack.github.io/debug-challenge/",
};

cards.push(project);

cards.map((item) => {
  cardsList.innerHTML += ``;
});
